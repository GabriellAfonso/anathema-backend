[pytest]
DJANGO_SETTINGS_MODULE = core.settings
python_files = tests.py test_*.py *_tests.py

# The websocket layer is async top to bottom, so plain `async def test_...`
# should just run without a per-test marker.
asyncio_mode = auto
