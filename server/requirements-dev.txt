# Ferramenta de desenvolvimento: as três portas de qualidade (pytest, mypy,
# black) e o que elas precisam. Nada aqui roda em produção.
#
# Instalar isto instala também as dependências de execução:
#     pip install -r requirements-dev.txt

-r requirements.txt

# Tipagem. django-stubs é o que ensina o mypy a ler Django; sem ele o plugin
# de mypy.ini não sobe.
django-stubs==6.1.0
mypy==2.3.1

# Testes.
pytest==9.1.1
pytest-django==4.14.0
pytest-asyncio==1.4.0
pytest-cov==7.1.0

# Formatação.
black==26.5.1
